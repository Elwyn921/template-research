# Experiment registry

Use the registry for **reportable** experiments, not every scratch notebook run.

A reportable run must have: dataset ID/version; configuration; Git commit; seed where applicable; primary result and diagnostics; and an explicit conclusion.

Suggested ID: `EXP-YYYYMMDD-###`.
