# Project Profiles Blueprint

Profile definitions are canonical in `tool_fabric/registry/profiles.yaml`.

This directory is for explanatory blueprint notes only. It must not duplicate
active profile routing rules.
