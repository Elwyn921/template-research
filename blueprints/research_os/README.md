# ResearchOS Blueprint

Reusable bootstrap guidance for downstream ResearchOS project structure.

Use `scripts/bootstrap_research_os.py` to create the directory skeleton, then
use `scripts/export_project_capabilities.py` to copy selected Foundry assets
under `08_tools/foundry/`.
